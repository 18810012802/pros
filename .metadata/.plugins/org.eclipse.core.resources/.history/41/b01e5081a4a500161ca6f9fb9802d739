$(function(){
	//alert(" mmmm")
	/*$.message.defaults = {
		ok:'是',
		cancel:'否'
	};*/
	
	/*$.message.alert('警告框','这是一个提示！','info',function(){
		alert('警告');
	});*/
	$.message.confirm('确认框','你真的要删除吗',function(flag){
		if(flag){
			alert('删除成功');
		}
	});
	/*$.messager.promt('提升框','请输入你的名字！',function(content){
		if(content){
			alert(content);
		}
	});
	$.message.progress({
		title:'执行中',
		msg:'努力上传中',
		text:'{value}%',
		interval:100,
		
		
	});
	//console.log($.message.progress('bar'));
	//console.log($.message.progress('close'));
	$.message.show({
		title:'我的消息',
		msg:'消息5秒关闭',
		timeout:5000,
		showType:'fade',
		style:{
			top:0,
		},
		
	});*/
	
	
});
	